/**
 * Re-exported from the design system so there is exactly one merge
 * configuration in the monorepo. A second copy would drift from the preset's
 * type scale and reintroduce the silent class-dropping it exists to prevent.
 */
export { cn } from '@reef-technologies/ui';
