{
  "name": "reef_technologies",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20.12.0",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "boundaries": "node scripts/check-boundaries.mjs",
    "contracts:export": "pnpm --filter @reef-technologies/contracts export:schema",
    "db:generate": "pnpm --filter @reef-technologies/api db:generate",
    "db:migrate": "pnpm --filter @reef-technologies/api db:migrate",
    "infra:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d",
    "infra:down": "docker compose -f infrastructure/docker/docker-compose.yml down",
    "verify": "pnpm boundaries && pnpm typecheck && pnpm lint && pnpm test",
    "test:integration": "turbo test:integration"
  },
  "devDependencies": {
    "@reef-technologies/eslint-config": "workspace:*",
    "@reef-technologies/tsconfig": "workspace:*",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3",
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  }
}
